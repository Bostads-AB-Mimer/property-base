model Collection {
  @@map("tvcln")
  collectionId String @id(map: "pktvcln") @map("keytvcln") @db.Char(15)
  name String @unique(map: "aktvcln") @map("caption") @db.VarChar(30)
  sortOrder Int @default(0, map: "DF__tvcln__sortorder__6E9DC858") @map("sortorder") @db.SmallInt
  timestamp String @map("timestamp") @db.Char(10) // TODO: consider changing timestamp later when knowing more about its usage 

  product Product[] @relation("tvprdtvcln") // TODO: Verify that this relation matches corresponding collectionId in Product model if necessary
}