model MeasurementType {
  @@map("cmvat")

  measurementTypeId String @id(map: "pkcmvat") @map("keycmvat") @db.Char(15)
  unitId String? @map("keycmuni") @db.Char(15) // TODO: Confirm the unit relationship in backward pass
  name String @map("caption") @db.VarChar(30)
  mask String? @map("inputmask") @db.VarChar(30)
  locked Int @default(0, map: "DF__cmvat__locked__208F4951") @map("locked") @db.TinyInt
  systemStandard Int @default(0, map: "DF__cmvat__repabkod__21836D8A") @map("repabkod") @db.TinyInt
  timestamp String @map("timestamp") @db.Char(10) // TODO: Evaluate the usage of timestamp later

  contractPrices ContractPrice[] @relation("fkavarkkeycmvat")
  measurementTypeLinks MeasurementTypeLink[] @relation("fkcmvapkeycmvat")
  plannedMaintenances PlannedMaintenance[] @relation("fkpuundkeycmvat")
  maintenancePackageContents MaintenancePackageContent[] @relation("fkskpinkeycmvat")
  periodicWorks PeriodicWork[] @relation("fkskundkeycmvat")

  // Relations that require further renaming
  // TODO: Rename cmart when available
  // cmart_cmart_keycmvatTocmvat cmart[] @relation("cmart_keycmvatTocmvat")
  // cmart_cmart_keycmvat2Tocmvat cmart[] @relation("cmart_keycmvat2Tocmvat")
  
  // TODO: Rename hyinf when available
  // hyinf_hyinf_keycmvatTocmvat hyinf[] @relation("hyinf_keycmvatTocmvat")
  // hyinf_hyinf_keycmvat2Tocmvat hyinf[] @relation("hyinf_keycmvat2Tocmvat")
  // hyinf_hyinf_keycmvat3Tocmvat hyinf[] @relation("hyinf_keycmvat3Tocmvat")
  // hyinf_hyinf_keycmvat4Tocmvat hyinf[] @relation("hyinf_keycmvat4Tocmvat")

  // TODO: Rename puatg when available
  // puatg_puatg_keycmvatTocmvat puatg[] @relation("puatg_keycmvatTocmvat")
  // puatg_puatg_keycmvat2Tocmvat puatg[] @relation("puatg_keycmvat2Tocmvat")

  @@index([unitId], map: "fkcmvatcmuni")
  @@index([name], map: "incmvat1")
}