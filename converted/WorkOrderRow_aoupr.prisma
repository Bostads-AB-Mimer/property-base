model WorkOrderRow {
  @@map("aoupr")
  
  workOrderRowId String @id(map: "pkaoupr") @map("keyaoupr") @db.Char(15)
  workOrderId String @map("keyaoupp") @db.Char(15)
  propertyObjectId String? @map("keyaopla") @db.Char(15)
  buildingPartId String? @map("keyaobdl") @db.Char(15)
  detailId String? @map("keyaodet") @db.Char(15)
  faultTypeId String? @map("keyaofel") @db.Char(15)
  actionId String? @map("keyaoatg") @db.Char(15)
  linkedFieldId String? @map("keycode") @db.Char(15) // TODO: Re-assess naming as more context is understood
  linkedFieldId2 String? @map("keycode2") @db.Char(15) // TODO: Re-assess naming as more context is understood
  description String? @map("caption") @db.VarChar(250)
  deviationNote String? @map("devnote") @db.VarChar(1000) // Assuming 'Avvikelse' translates to 'deviationNote'
  status Int @default(0, map: "DF__aoupr__uprstatus__7016FA7F") @map("uprstatus") @db.TinyInt // Assuming 'Genomförd' translates to 'status'
  completedDate DateTime? @map("perfdate") @db.Date // Assuming 'Completed date' translates to 'completedDate'
  toBeDebited Int @default(0, map: "DF__aoupr__debit__710B1EB8") @map("debit") @db.TinyInt
  printOrder Int @default(0, map: "DF__aoupr__printsort__71FF42F1") @map("printsort") @db.TinyInt
  timestamp String @map("timestamp") @db.Char(10) // TODO: change timestamp later when knowing more about its usage 
  
  action Action? @relation(fields: [actionId], references: [actionId], onDelete: NoAction, onUpdate: NoAction, map: "fkaouprkeyaoatg")
  buildingPart BuildingPart? @relation(fields: [buildingPartId], references: [buildingPartId], onDelete: NoAction, onUpdate: NoAction, map: "fkaouprkeyaobdl")
  detail Detail? @relation(fields: [detailId], references: [detailId], onDelete: NoAction, onUpdate: NoAction, map: "fkaouprkeyaodet")
  faultType FaultType? @relation(fields: [faultTypeId], references: [faultTypeId], onDelete: NoAction, onUpdate: NoAction, map: "fkaouprkeyaofel")
  propertyObject Placement? @relation(fields: [propertyObjectId], references: [propertyObjectId], onDelete: NoAction, onUpdate: NoAction, map: "fkaouprkeyaopla")
  workOrder WorkOrder @relation(fields: [workOrderId], references: [workOrderId], onDelete: Cascade, onUpdate: NoAction, map: "fkaouprkeyaoupp")
  skaok skaok[] // TODO: Determine what 'skaok' represents and consider renaming

  @@index([actionId], map: "fkaoupraoatg")
  @@index([buildingPartId], map: "fkaoupraobdl")
  @@index([detailId], map: "fkaoupraodet")
  @@index([faultTypeId], map: "fkaoupraofel")
  @@index([propertyObjectId], map: "fkaoupraopla")
  @@index([workOrderId], map: "fkaoupraoupp")
  @@index([linkedFieldId], map: "fkaouprcode")
}