model BillingPaymentMethod {
  @@map("cmdbm")

  methodId String @id(map: "PK__cmdbm__6BAFC5BE") @map("keycmdbm") @db.Char(15)
  routineId String @map("keycmrtn") @db.Char(15) // TODO: Confirm routine name and mapping after backward pass
  
  debitPrinciple Int @map("debitprinc") @db.SmallInt
  name String? @map("name") @db.Char(30)
  identifier String? @map("id") @db.Char(20)
  giro String? @map("giro") @db.Char(20)
  bic String? @map("bic") @db.Char(11)
  iban String? @map("iban") @db.Char(42)
  timestamp String @map("timestamp") @db.Char(10) // TODO: Determine the specific purpose and usage context

  invoices Invoice[] @relation("fkkrfkhkeycmdbm") // Convert this using Invoice model name
  
  @@index([routineId], map: "fkcmdbmcmrtn")
}