model ResourceGroup {
  @@map("cmrgr")
  
  resourceGroupId String @id(map: "pkcmrgr") @map("keycmrgr") @db.Char(15)
  planningTypeId String? @map("keybaret") @db.Char(15) // TODO: confirm if "Resursplaneringstyp" translates directly to "planningTypeId"
  code String @map("code") @db.VarChar(10)
  name String? @map("caption") @db.VarChar(30) // TODO: confirm if "Benämning" translates directly to "name"
  notificationMode Int @default(0, map: "DF__cmrgr__resmail__0A06B702") @map("resmail") @db.TinyInt
  emailNotification Int @default(0, map: "DF__cmrgr__mailnoti__0AFADB3B") @map("mailnoti") @db.TinyInt
  includedInPlanboard Int @default(0, map: "DF__cmrgr__incplanbr__0BEEFF74") @map("incplanbrd") @db.TinyInt
  timestamp String @map("timestamp") @db.Char(10) // TODO: confirm if 'timestamp' needs a different naming convention

  appointments Appointment[] @relation("fkaocapkeycmrgr") // aocap
  reminderTransactions ReminderTransaction[] @relation("fkaormlkeycmrgr") // aorml
  workOrders WorkOrderRow[] @relation("fkaouppkeycmrgr") // aoupp
  resourcePlannings ResourcePlanning[] @relation("fkbarekkeycmrgr") // barek
  resourceInResourceGroups ResourceInResourceGroup[] @relation("fkcmrgkkeycmrgr") // cmrgk
  resourcePlanningType ResourcePlanningType? @relation("fkcmrgrkeybaret") // baret
  apartmentInspections ApartmentInspection[] @relation("fklbbeskeycmrgr") // lbbes
  periodicWorks PeriodicWork[] @relation("fkskundkeycmrgr") // skund
  
  @@index([planningTypeId], map: "fkcmrgrbaret")
}